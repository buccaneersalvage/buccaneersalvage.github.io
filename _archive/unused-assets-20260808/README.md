# Unused assets archived 2026-08-08

Moved out of the public hub tree so `deploy-buccaneer-pages` (rsync excludes `_archive`) does not ship them to GitHub Pages.

Not deleted — restore if a build script or design pass needs a master.

`fitment-db.json` was restored to `assets/` for `scripts/build_fitment.py` but is rsync-excluded from public deploy.
