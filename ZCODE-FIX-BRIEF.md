# Fix brief — BuccaneerSalvage hub (from arch review 2026-08-21)

Repo root: `~/sites/buccaneersalvage-hub` (this file's directory)
Source review: `~/kimi-work/buccaneer-arch-review-2026-08-21.md`

A prior review (Kimi) audited this static site. I independently re-verified
every claim below against the actual files — all confirmed accurate. This
brief covers the review's "Recommended fix order" (items 1–6).

## Hard rules — read first

1. **DO NOT edit, create, or delete any file.** Output your proposed changes
   as unified diffs (or clearly-labeled full-file blocks for new files) in
   your chat response only. A human will review every diff before anything
   is applied.
2. **DO NOT run git, deploy-buccaneer-pages, or any script that writes to
   disk or touches the network**, except read-only inspection.
3. Touch only the files named in each task. No drive-by refactors, no
   "while I'm here" cleanups, no renaming, no touching unrelated CSS/JS.
4. If a task turns out to need more than a small, mechanical change (see
   Task 4), give a short implementation **plan** instead of a diff — do not
   attempt to generate 296 images or run a batch job.
5. This site is a git repo (`git init` was just run, HEAD = clean snapshot),
   so nothing here is unrecoverable — but stick to the rules above anyway,
   output only, no direct edits.

---

## Task 1 — Deploy-stamp script skips `shame/*.html`

File: `scripts/stamp_sri.py`, function `html_targets()` (around line 89-96):

```python
def html_targets() -> list[Path]:
    files = sorted(ROOT.glob("*.html"))
    pdir = ROOT / "p"
    if pdir.is_dir():
        files.extend(sorted(pdir.glob("*.html")))
    builder = ROOT / "scripts" / "build_static_pdps.py"
    if builder.is_file():
        files.append(builder)
    return files
```

It globs `ROOT/*.html` and `p/*.html` but never `shame/*.html`, so SRI hash
stamping silently skips `shame/devitalized.html` and
`shame/carpartsforsale2013.html`. Add a block for the `shame/` directory,
same pattern as the existing `p/` block. Give me the diff.

## Task 2 — Stale sitemap entry + orphaned thumbs for a removed product

`sitemap.txt:19` lists `https://buccaneersalvage.github.io/p/R6VO2MARXN7GRGTMXVGABLHT.html`
— that page no longer exists (`p/R6VO2MARXN7GRGTMXVGABLHT.html` is gone).
Orphaned files still on disk for that same product ID:
- `assets/product-thumbs/R6VO2MARXN7GRGTMXVGABLHT.webp`
- `assets/pdp-gallery/R6VO2MARXN7GRGTMXVGABLHT/` (directory)

Give me: (a) the diff removing that one line from `sitemap.txt`, (b) the
list of file/dir paths to delete (I'll delete them by hand, don't try to
delete anything yourself).

## Task 3 — Duplicate `id="destinations"` in `index.html`

Two elements share this id:
- `index.html:307` — `<section class="section" id="destinations">` (the
  intended target — nav link `<a href="#destinations">Ports of call</a>` at
  line 255, and JSON-LD `"@id": "https://buccaneersalvage.github.io/#destinations"`
  at line 188, both point here)
- `index.html:482` — `<footer role="contentinfo" class="footer" id="destinations">`
  (accidental duplicate — I checked, nothing anywhere in the repo links to
  or scripts against `#destinations` expecting the footer, only the section)

Fix: remove `id="destinations"` from the footer tag at line 482 only. Leave
the section's id alone. Give me the diff (one line changes).

## Task 4 — PDP hero image is full-res S3 original, not the local WebP

All 296 `p/*.html` pages load the hero/stage image straight from
`https://items-images-production.s3.us-west-2.amazonaws.com/.../original.jpeg`
(see `p/*.html` around line 72, `<img id="pdpMainImage" ...>`), generated by
`scripts/build_static_pdps.py`. 251 of those products *do* have a local
optimized WebP gallery under `assets/pdp-gallery/<ID>/`, but — per the
generator's own comment ("Skip photo-01, usually the Square hero") — that
local gallery only ever contains images `02.webp` onward. There is no local
`01.webp` (optimized hero) for any product; the hero always comes straight
from S3 full-res, even for the 251 products with a local gallery.

**Don't generate images or touch 296 files.** Just answer: what's the
smallest correct change to `scripts/build_static_pdps.py` so that, for
products which already have a `assets/pdp-gallery/<ID>/` directory, a
`01.webp` optimized hero gets generated (same resize/webp pipeline as
`ensure_listing_gallery()` uses for 02+) and used as `img` for the main
stage tag — falling back to the current S3-original behavior for the ~45
products with no gallery folder. Give me a short plan (function to add/
modify, where it hooks in) and a diff for `build_static_pdps.py` only. Do
not attempt to run it or touch any `p/*.html` file directly — the diff to
the generator is enough, re-running it is a separate step I'll do myself.

## Task 5 — No `404.html`, no privacy page

Neither file exists at repo root. Give me:
- `404.html` — match the site's existing look (reuse `styles.css`, same
  nav/footer structure as `index.html`), same CSP meta tag as other root
  pages, a link back to `index.html` and `store.html`, `<meta name="robots"
  content="noindex, follow">`, `<title>` in the site's voice (pirate/salvage
  branding — see `index.html` copy for tone).
- `privacy.html` — minimal real privacy page: what's collected (Square
  checkout handles payment off-site, no accounts, no tracking beyond
  standard GitHub Pages access logs — check `index.html`/`store.html` for
  what's actually true before writing claims), contact info (`(570)
  468-2901`, `jollyroger1480@gmail.com` — pull these from `store.html`'s
  JSON-LD if you need to confirm), same CSP/nav/footer pattern.

Give me both as full new-file blocks, clearly labeled with their filename.

## Task 6 — Inline style blocked by CSP on `shame.html:77`

```html
<h2 class="st-title" style="margin-bottom:1.25rem;">Entries</h2>
```

This is blocked live by the page's own `style-src 'self'` CSP. `.st-title`
(defined in `styles.css` ~line 1439) is a **shared** class also used on the
`<h1>` on this same page and on `shame/devitalized.html` and
`shame/carpartsforsale2013.html` — so don't add `margin-bottom` to
`.st-title` itself, that'd change all four usages. Add a small modifier
class instead (e.g. `.st-entries-title { margin-bottom: 1.25rem; }`) and
apply it alongside/instead of the inline style on that one `<h2>`. Give me
the diff for both `styles.css` (new rule) and `shame.html` (the one line).

---

## Output format

For each task, reply with a `### Task N` heading followed by either a
```diff
fenced diff block
```
or, for Task 5's new files, a fenced block labeled with the filename. No
prose explanation needed beyond a one-line note if something's ambiguous.
